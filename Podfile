# Uncomment the next line to define a global platform for your project
# use "arch -x86_64 pod install" in terminal to install pods
platform :ios, '10.0'

target 'TeacherScene' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TeacherScene
pod "SCPageControl"
pod 'SkeletonView'
end

